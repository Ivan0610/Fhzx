这个工作流程将在发布时构建node.应用程序并将其推入Azure Web应用程序。##配置此工作流:## 1。使用Azure发布配置文件的值在存储库中设置一个名为AZURE_WEBAPP_PUBLISH_PROFILE的秘密。# # 2。更改AZURE_WEBAPP_NAME、AZURE_WEBAPP_PACKAGE_PATH和NODE_VERSION环境变量的值(如下所示)。# #在GitHub行动Azure的更多信息,请参考<hl>余见好</h1>
行动工作流部署到Azure,引用https://github.com/Azure/actions-workflow-sampleson发布:类型:[产生]env: AZURE_WEBAPP_NAME: your-app-name #设置此应用程序的名称AZURE_WEBAPP_PACKAGE_PATH:”。' #将此设置为web应用程序项目的路径，默认设置为存储库根节点版本:'10。x' #将其设置为节点版本，以使用jobs: Build -and- Deploy: name: Build and Deploy run -on: ubuntu-latest steps: - uses: actions/checkout@v2 - name: Use node .js ${{env。NODE_VERSION}}使用:actions/setup-node@v1和:node-version: ${{env。NODE_VERSION}} -name: npm install, build, and test run: # build and test test the project: # deploy to Azure Web App. npm install npm run build—if-present npm run test—if-present—name: ' deploy to Azure WebApp'使用:Azure /webapps-deploy@v2 with: app-name: ${{env。AZURE_WEBAPP_NAME}}发布配置文件:${{秘密。AZURE_WEBAPP_PUBLISH_PROFILE}}包:${{env。AZURE_WEBAPP_PACKAGE_PATH}}
